// Import a Scala object or Java class with static methods.
// Its methods can be called using Foo::bar(args...).
import object baz.Foo

// Defines a component with two variables in its state.
component Something (active: 0..1, queue: 0..100)
    // Optional initial value for a variable.
    queue := 50

    component function on uses 5 time 8 energy
        active := 1
    end function

    component function assign uses 5 time 1 energy;

    component function blaat(X) uses 65 energy := X + 10

    function oof(X) := baz.Foo::boo(X)

    // This ...
    function foo(X) := X ^ 2

    // ... is syntactic sugar for this:
    function foo(X)
        foo := X ^ 2
    end function



    // This ...
    function bar(X); // The semicolon is required here!

    // ... is syntactic sugar for this:
    function bar(X)
        skip
    end function

end component

